# 一页纸总结 · TPS62933 (24V→12V/3A) 仿真

> 读这一页就够。细节全在 `results/` 与 `docs/`。最后更新：2026-09-24 13:05（`rad-conv` 已回；`eddy-redo` 待回）。

## 一句话
**能用的是"趋势/机理"；"绝对数字"基本都不能信；"电感下挖铜"目前给不出可信利弊。**

## ✅ 可信的结论（3 条）
1. **π 型输入滤波有效**：挡掉约 **58 dB** 的 DM 传导干扰（去掉它基波从 18.5 → 96 dBµV，严重超标）。
2. **共模(CM)量级 >> 差模(DM)**：CM 比 DM 高约 **一个量级**（~71 dB），**EMI 瓶颈在 CM，不在 DM**；π 滤波治不了 CM。
3. **电感下的涡流损耗本来就极小**：≪ DCR 铜损（**<0.1%**）→ **对温升无可测影响**（这条对上界外推也成立，较稳）。

## ❌ 不能信的红线（3 条）
1. **辐射的绝对幅值**：已收敛，但**绝对电平取决于 DM 源模型（Ipk 4.3A / tr 5ns）+ 简化几何** → **仍不可照搬**；三版差异**已判定为"无可分辨"**（见上）。
2. **传导 CM 的绝对数值**：耦合元件**定义错了**（把板内电容当共模电容），**绝对值不可用**，方向上之前偏保守。
3. **fea2/fea4 场地图的绝对数**：损耗**公式用错**、B/J **不自洽**、**未收敛** → 只能看 |J| **形态**。

## 🆕 辐射已收敛（`results/13-rad-converged/`）
- 修掉根因（几何没落在网格上 → 微元胞 → **源脉冲被截断**）→ **收敛达标**（相邻网格 Δ=0.12dB，末态能量 −48dB）。
- **三版差异：无可分辨**（100/300MHz 差 0.1~0.3dB；30MHz 的 2.1dB 落在 ±2.4dB 网格带内）⇒ **"抠掉 ≈ 没抠" 成立**。

## ⏳ 还在跑
- `eddy-redo`：修好涡流激励 → 出**可信的涡流损耗(W)**。

## 🔧 设计建议（现阶段）
- **别为 EMI 去挖电感下的铜**：损耗收益≈0，EMI 收益证不了，还漏掉地平面。
- 你的板 **无 PE** → CM 无硬通路，**真实 CM 大概率没模型说的那么糟**；**先别慌改设计**。
- 真要压 EMI，优先：**缩短 SW 热回路 / 加共模扼流圈 / 改善输入滤波布局**（等 CM 重做后定方向）。

## 🔬 还缺什么（要实测才能定）
SW 节点边沿速率 & 振铃、源阻抗 S(W)、层叠 εr、以及**实测传导/辐射**（仿真只能给趋势）。
