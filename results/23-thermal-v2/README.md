# 23 — 板级稳态热仿真 v2（步骤1：铜面热扩散）

修正 v1 四缺陷：①发热改**封装体内体积热源**（去锐边 patch 奇异）②加**封装 θJB 链**（U21 用 TI SLUSEA4D **θJB=19.3 °C/W**，D 级）③h **随 ΔT 变**（Incropera 关联式，10→13，E）④**导线/端子导热**（2×0.75mm²×10cm，E）。
**工具坑**：本 Elmer 构建静默忽略 BodyForce `Heat Source`，仅 `Volumetric Heat Source` 生效。

**收敛**：板均温 **96.9–97.4 °C（Δ<0.5%，B ✔）**；**U21/D1 局部峰不收敛**（272→294→334 °C）→ **C 不可引用**（与 v1 的 302→347→389 同性质；"v2 低 14%"**不可读作真实改善**）。
**守恒**：Q_out=5.00W，失衡 0.0%（对流 3.05 + 辐射 1.81 + 导线 0.31）。

**可信结论（B）**：
- 板均温 ~97 °C，**热点在 U21**。
- 用 θJB 链折算 **U21 结温 ≈ 板温 + 2.97W×19.3 ≈ 150–160 °C** → **已达/超芯片 150 °C 上限**。
- 因此：**13cm² 的 1oz 双面板、ΣQ=5W、无风冷无散热器，压不住 U21 结温** —— 该结论**依赖 S 级假设「总损 5W」**，须实测 P_in。
**GAPS**：U21/D1 结温需**专门子模型**才能收敛；**未建 166 过孔**（真 k_z 更高 → 当前局部峰偏高，交步骤2）；D1/L2/L1 封装 θ 无手册（E）；未找到 L1/L2 热参数手册。
图：`figures/fig_thermal_map_v2.png`、`fig_thermal_sensitivity_v2.png`。
